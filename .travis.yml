language: java
jdk:
  - oraclejdk7
before_install: ./ci/before_install.sh
install: 
  - "echo we do not need install"
script: ./ci/script.sh
after_script: ./ci/after_script.sh  
env:
  - MAVEN_OPTS="-Xms512m -Xmx512m -XX:PermSize=256m -XX:MaxPermSize=256m"
cache:
  directories:
  - $HOME/.m2

