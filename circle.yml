machine:
  timezone: America/Sao_Paulo
  java:
    version: oraclejdk8
dependencies:
  override:
    - ./ci/ci-deps.sh
test:
  override:
    - ./ci/ci-test.sh
deployment:
    prod:
        branch: [ master ]
        commands:
           - ./ci/ci-release-snapshot.sh
