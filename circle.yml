machine:
  timezone: America/Sao_Paulo
  java:
    version: oraclejdk7
dependencies:
  override:
    - ./ci/ci-deps.sh
test:
  override:
    - ./ci/ci-test.sh
deployment:
    snapshot:
        branch: [ snapshot ]
        commands:
           - ./ci/ci-release-snapshot.sh
